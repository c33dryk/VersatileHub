# =============================================================================
# INFRASTRUCTURE - Configuración de Entorno
# =============================================================================
# Este archivo configura la infraestructura base (Traefik, Portainer, etc.)
#
# IMPORTANTE:
# - Estas variables son para la INFRAESTRUCTURA
# - Las variables de servicios están en /opt/ATS/VersatileHub/.env
# - Copiar este archivo: cp .env.example .env
#
# =============================================================================

# === GENERAL ===
TZ=America/Argentina/Buenos_Aires

# === ACME / LET'S ENCRYPT ===
ACME_EMAIL=info@versatile.ar

# === CLOUDFLARE ===
# Para desafío DNS de certificados SSL
CLOUDFLARE_EMAIL=tu-email@cloudflare.com
CLOUDFLARE_API_KEY=tu-cloudflare-api-key-global

# Alternativamente puedes usar API Token en vez de Global API Key:
# CLOUDFLARE_DNS_API_TOKEN=tu-cloudflare-dns-api-token
# CLOUDFLARE_ZONE_API_TOKEN=tu-cloudflare-zone-api-token

# === TRAEFIK DASHBOARD ===
# 🔒 ACCESO SEGURO: Dashboard NO expuesto públicamente
# Acceso solo vía SSH tunnel desde tu máquina local:
#   ssh -L 8080:localhost:8080 usuario@servidor
#   Luego abrir: http://localhost:8080
TRAEFIK_DASHBOARD_USER=admin

# === PORTAINER ===
# 🔒 ACCESO SEGURO: Portainer NO expuesto públicamente
# Acceso solo vía SSH tunnel desde tu máquina local:
#   ssh -L 9000:localhost:9000 usuario@servidor
#   Luego abrir: http://localhost:9000
# Primera vez: crear usuario admin con password fuerte

# =============================================================================
# NOTAS
# =============================================================================
#
# 1. CLOUDFLARE API KEY vs API TOKEN:
#    - Global API Key: Más simple pero menos seguro (acceso completo)
#    - API Token: Más seguro, permisos específicos (recomendado)
#
# 2. CERTIFICADOS SSL:
#    - cloudflare: Desafío DNS (funciona detrás de firewall)
#    - letsencrypt: Desafío HTTP (requiere puertos 80/443 abiertos)
#
# 3. ACCESO SEGURO (SSH TUNNELS):
#    - Traefik Dashboard: ssh -L 8080:localhost:8080 user@server
#    - Portainer: ssh -L 9000:localhost:9000 user@server
#    - NO están expuestos públicamente por seguridad
#
# 4. CREAR ALIAS SSH (opcional, en ~/.ssh/config):
#    Host versatile
#      HostName IP_DEL_SERVIDOR
#      User tu_usuario
#      LocalForward 8080 localhost:8080
#      LocalForward 9000 localhost:9000
#
#    Luego solo: ssh versatile
#
# =============================================================================
